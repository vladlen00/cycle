# HANDOVER: мини-апп "Цикл"

**Обновлён:** 16 сентября 2026
**Статус:** в проде, работает с реальными подписчицами

Документ описывает фактическое состояние проекта. Правила для агента лежат отдельно, в `CLAUDE.md`, он грузится автоматически. Хронология правок в `git log`.

## 1. Что это и где живёт

Telegram Mini App для трекинга менструального цикла, часть Студии Ирены Пол. Аудитория примерно 600 женщин платного канала.

- Точка входа: `t.me/relax2000_bot/cycle`, слаг `cycle` зарегистрирован в @BotFather
- Вторая точка входа: карточка "Цикл" в хаб-аппе studio, соседний репозиторий
- Фронт: GitHub Pages, репозиторий `vladlen00/cycle`, ветка `main`, адрес `https://vladlen00.github.io/cycle/`
- Бэкенд: Supabase, проект `kjzxrpwqyyjcykwbqskn`, функция `cycles-api`, таблицы `cycles` и `cycle_symptoms`

**Деплой фронта:** коммит в `main`, GitHub Pages пересобирается за 1-3 минуты. После этого обязательно поднять `?v=` в адресе мини-аппа в @BotFather, иначе WebView Телеграма отдаст старую страницу из кэша.

**Версии файлов внутри страницы** живут в `index.html` и поднимаются при каждой правке соответствующего файла.

## 2. Файлы

| Файл | Роль |
|---|---|
| `index.html` | Разметка: три экрана, шесть модалок, полоса "Поделиться", плавающая кнопка "+", карточка вопроса о длительности |
| `style.css` | Палитра в переменных, layout, компоненты, модалки |
| `app.js` | UI и оркестрация: состояние, рендер, обработчики, рисование картинки на canvas |
| `cycleCalc.js` | Чистая логика расчёта. Ни часов, ни DOM, `today` всегда приходит параметром |
| `auth.js` | Две двери входа, хранение токена, обёртка `CyclesApi` над `cycles-api` |
| `telegram-web-app.js` | Вендорный SDK Телеграма, не трогаем |
| `edge-functions/cycles-api/index.ts` | Копия задеплоенной функции для правок и ревью |
| `db/migrations/` | След применённых DDL |
| `CLAUDE.md` | Правила для агента, грузится автоматически |
| `_references/` | Локальные копии серверного кода. В `.gitignore`, не коммитится |

Файла `README.md` в репозитории нет.

## 3. Что приложение умеет сегодня

**Экран "Сегодня".** Кольцо с номером дня цикла и названием фазы, подпись с прогнозом следующей менструации, кнопка "Отметить менструацию".

- **Вопрос о длительности.** Длительность менструации почти никто не правил: в форме стояла пятёрка, на 15.09.2026 она была в 64% живых записей, и по оценке около трети всех записей неверны. Поэтому с 8 по 14 день цикла под кнопкой появляется карточка "Сколько дней шла менструация?": кнопки 3-7, тихая ссылка "другое" в обычную правку записи и крестик. Только если в текущей отметке стоит 5, другое число это уже выбор. Другое число правит одно поле `menstruation_length_days` частичным `update`, дальше тост "Готово: N дней" с кнопкой "Вернуть". То же число запрос не шлёт. Ответ, крестик или "другое" закрывают вопрос для этого цикла. Кнопки нарочно без акцента и текущее число не подсвечено: иначе его тычут как "ОК", лишь бы убрать карточку.
- **Первый заход в окне открывается на "Сегодня".** С отметками приложение стартует на календаре, и карточку никто бы не увидел. Раз за окно старт переносится на "Сегодня", дальше снова календарь. Если `localStorage` не пишется, старт не переносится вовсе, иначе каждое открытие стало бы заслоном.
- **Память вопроса** в `localStorage`, два ключа: `cycle.lenAsk.done` (ответила, закрыла или ушла в форму) и `cycle.lenAsk.landed` (старт уже переносили). В обоих id цикла, новый цикл открывает вопрос сам. Память живёт на устройстве: на втором устройстве вопрос повторится один раз. Форма тоже ставит "ответила", но только если длительность вписана руками: сохранение правки ради даты или заметки пятёрку не подтверждает.
- **На низком экране кольцо ужимается,** пока висит карточка: ровно настолько, чтобы карточка встала без прокрутки, но не меньше 140 пикселей. Ниже 220 номер дня и фаза мельче. Ниже 170 название фазы спрятано, остаётся номер дня: фаза стоит под центром, где кольцо уже, длинное "ФОЛЛИКУЛЯРНАЯ" упиралось в дорожку, а запас при уменьшенном шрифте выходил в несколько пикселей, и проверить его на живом iPhone нечем. Фаза видна на календаре, ужатое кольцо бывает несколько дней за цикл и только на маленьких экранах. Размер меряется, а не угадывается (`fitRingToLengthAsk`), пересчёт на изменение окна и после загрузки шрифтов. На обычном экране всё помещается, и кольцо не меняется. Карточка ушла, кольцо снова обычное.

**Экран "Календарь".** Пятнадцать месяцев: два назад и двенадцать вперёд. Рисуются и без единой отметки менструации: иначе женщине, которая пришла отмечать самочувствие, не по чему тапнуть. Менструация рисуется кружком с числом, овуляция контурным сердцем, фолликулярная и лютеиновая точкой под числом. Прогнозные дни отличаются визуально от прожитых.

- **Липкая легенда сверху.** Пункт фазы виден, только если такая фаза реально нарисована в построенном календаре. При одной отметке типичная длина неизвестна, рисуется одна менструация, и в легенде одна строка. Пятая строка "Отмечено самочувствие" появляется, если в календаре есть хоть одна точка самочувствия. Нет ни одного пункта, прячется вся легенда: пустая белая полоса читалась бы как поломка.
- **Подсказка "Нажми на день, чтобы отметить самочувствие"** стоит внутри легенды и видна, пока у женщины нет ни одной отметки самочувствия. Над списком месяцев её ставить нельзя: календарь при открытии прокручивается к текущему месяцу, и подсказка уезжала бы раньше, чем на неё посмотрят.
- **Заголовок месяца липнет под легендой** в пределах своего месяца, следующий месяц вытесняет его сам. Дни недели не липкие: их порядок всегда один. Высота легенды меняется, поэтому её меряет `measureLegendHeight` и кладёт в `--legend-h`. От неё отступают липкий заголовок и посадка прокрутки. В `scroll-margin-top` учтён ещё отступ `.app-main`: липкий элемент его учитывает, а `scrollIntoView` нет.
- **Тап по дню открывает превью:** дата с днём недели, фаза, номер дня цикла, для дней без данных честное "Нет данных". Только чтение. У дня с реальной отметкой есть кнопка "Изменить запись". Раньше тап открывал форму СОЗДАНИЯ с этой датой, и "Сохранить" читалось как "ОК": подписчица так завела случайную отметку, которая сдвинула ей весь прогноз. Создание записи живёт только на кнопке "+" и на главном экране. Для прожитого дня в превью есть блок "Самочувствие" с тем, что отмечено, и кнопкой в шторку отметки.
- **Полоса "Поделиться датами цикла"** отдельной строкой над навигацией. Видна только на календаре и только когда есть хотя бы одна запись. Выбор периода 1, 3 или 6 месяцев, дальше приложение рисует картинку и открывает её на весь экран.
- **Плавающая кнопка "+"** создаёт запись. Пока видна полоса "Поделиться", кнопка поднята на её высоту.

**Экран "История".** Последние шесть циклов: дата старта, длительность, отклонение от средней и лента дней с цветами фаз.

**Тост отмены.** После успешного создания записи пять секунд висит тост "Отметка добавлена" с кнопкой "Отменить". Она удаляет только что созданную запись и перерисовывает экран.

**Самочувствие по дням.** Женщина отмечает симптомы (19 вариантов), выделения (9) и свой текст до 300 знаков, чтобы показать гинекологу. Одна строка на день.

- **Вход только из превью дня,** кнопка "Отметить самочувствие" или "Изменить самочувствие". Само превью остаётся только для чтения. У будущего дня блока нет: отмечают то, что было.
- **Шторка снизу,** "Отмена" и "Сохранить" прилипают к низу и видны при любой прокрутке. Сохранение только кнопкой, не по тапу. Если есть несохранённые выборы, закрытие переспрашивает "Не сохранять отметки?" внутри самой шторки.
- **"Всё в порядке" и "Выделений нет" это отметки, а не пустота,** и с другими вариантами своего списка не совмещаются. Проверка в трёх местах: приложение, функция, база.
- **Снятые все выборы удаляют день целиком,** удаление настоящее, не мягкое: это медицинские данные. Тост "Отметки дня удалены".
- **Точка в правом верхнем углу клетки** у любого отмеченного дня, включая "Всё в порядке". Ставится поверх уже построенного календаря (`paintSymptomDots`), без пересборки. Пересборка прокрутила бы женщину к текущему месяцу, пока она читает свой.
- **Отметки грузятся параллельно с циклами** и первый показ не задерживают. Пока статус загрузки не `ok`, нарисованные точки не трогаются: при сбое или повторной загрузке исчезающие точки читались бы как потерянные отметки. Превью в это время честно пишет "Загружаем отметки" или "Не удалось загрузить отметки" с кнопкой "Повторить", и в шторку не пускает: сохранение заменяет день целиком и затёрло бы отметки, которых экран не видел.
- **Отметки живут только в памяти,** в `localStorage` не пишутся: он на `vladlen00.github.io` общий у всех мини-аппов, медицинским данным там не место.
- **ИИ-подружка отметок не видит,** сознательно: её ответы легли бы в `chat_history`, а к ней есть открытый вопрос доступа.

**Форма отметки задним числом.** Если дата начала на 7 и больше дней раньше сегодня, поле длительности пустое и обязательное, подпись "Сколько дней шла?": длина уже известна, и подставленная пятёрка стала бы неправдой. На 15.09.2026 у 113 из 295 женщин последняя отметка была внесена уже после расчётного конца менструации. Граница совпадает с началом окна карточки на "Сегодня": отметка задним числом это день цикла 8 и позже, поэтому поздняя отметка получает вопрос в форме, своевременная карточку. Работает только при создании, у правки число своё. Число, вписанное руками, смена даты не затирает.

**Картинка для отправки.** Рисуется в памяти и никуда не загружается. Геометрия одна на все периоды, масштаб подбирается так, чтобы длинная сторона не вышла за 1280 пикселей: до этого размера Telegram ужимает фото при обычной отправке. Иерархия намеренная: менструация это круг, заливка у прожитого дня с отметкой и контур у будущего прогнозного; овуляция это маленькая точка под числом; фолликулярная и лютеиновая не рисуются вовсе. Легенда из двух пунктов плюс строка про заливку и контур. Фазы берутся тем же вызовом `getCalendarPhases` и с теми же входами, что и экранный календарь, цвета читаются из тех же переменных CSS: картинка не может разойтись с приложением.

Отправка идёт через `navigator.share`, кнопка показывается только если браузер подтвердил поддержку файлов. Кнопки "Сохранить" нет сознательно: подтвердить работу скачивания внутри WebView нечем, а кнопка, которая молча не срабатывает, хуже её отсутствия. Телеграмовские методы отправки тут не годятся: `downloadFile` и `shareToStory` требуют адрес по https, `shareMessage` требует подготовленное ботом сообщение. Всё это упирается в хостинг файла и правку бэкенда.

## 4. Расчёт фаз

Всё в `cycleCalc.js`:

- Лютеиновая фаза 14 дней, день овуляции равен средней длине минус 14, окно овуляции три дня
- Менструация это дни с первого по `menstruation_length_days`, по умолчанию 5
- Типичная длина цикла: интервалы между соседними отметками, отсев значений вне 15-45 дней, МЕДИАНА по последним шести валидным. Валидных нет - функция возвращает `null`, и подстановки 28 больше нигде нет: каждый читатель обязан показать, что длина неизвестна
- `getCalendarPhases` сворачивает номер дня только для будущего и только для самого нового цикла. Закрытые циклы и уже прожитые дни считаются без свёртки: при задержке выходит лютеиновая, и это правда, а не начало нового цикла

**Верхней границы у календаря нет.** Расчёт красит весь переданный диапазон, а `renderCalendar` передаёт конец последнего из пятнадцати месяцев. Клемпинга нет ни в UI, ни в расчёте. В памяти Claude лежит файл `cycle_app_calendar_upper_bound` с планом такого клемпинга: это нереализованный замысел, а не описание кода.

## 5. Инфраструктура

**Таблица `cycles`:**

| Колонка | Тип | Заметка |
|---|---|---|
| `id` | uuid | первичный ключ |
| `user_id` | text | Telegram user.id строкой, совпадает с полем `sub` в токене |
| `start_date` | date | дата начала менструации |
| `menstruation_length_days` | int | по умолчанию 5 |
| `notes` | text | необязательно |
| `created_at` | timestamptz | |
| `deleted_at` | timestamptz | мягкое удаление, у живых строк null |

Ограничение `cycles_user_date_unique` на паре `(user_id, start_date)`. RLS включён, но `cycles-api` ходит под `service_role` и обходит его, поэтому защита сделана в коде: явный фильтр по идентификатору из токена в каждом запросе.

В колонку `user_id` пишется поле `sub` из токена. При входе через Телеграм это Telegram user.id, при входе через веб UUID. На 16.09.2026 из 695 живых записей UUID у трёх.

**Таблица `cycle_symptoms`:**

| Колонка | Тип | Заметка |
|---|---|---|
| `id` | uuid | первичный ключ |
| `user_id` | text | то же, что в `cycles` |
| `date` | date | день отметки, не раньше 2020-01-01 |
| `symptoms` | text[] | коды симптомов |
| `discharge` | text[] | коды выделений |
| `note` | text | свой текст, 1-300 знаков или null |
| `created_at` | timestamptz | |
| `updated_at` | timestamptz | ставит `cycles-api` |

Одна строка на пару `(user_id, date)`. Пустой день не хранится, его удаляют. RLS включён без политик, гранты у `anon` и `authenticated` отозваны: дефолтные привилегии этой базы выдают им права на каждую новую таблицу, и одного RLS мало. Список допустимых кодов проверяет `cycles-api`, а не база: формулировки утверждает Ирена, и правка списка не должна требовать миграции. DDL в `db/migrations/20260915_create_cycle_symptoms.sql`.

**Функция `cycles-api`.** Действия `list`, `create`, `update`, `delete`, `symptoms_list`, `symptoms_save`. Настройка `verify_jwt` выключена, иначе шлюз Supabase заблокирует наш токен раньше, чем код функции до него дойдёт.

- `list` отдаёт только живые строки, по убыванию даты
- `create` это upsert по паре пользователь и дата с обнулением `deleted_at`: повторное добавление удалённой даты реанимирует старую строку, дубликата не появляется
- `delete` мягкий, проставляет `deleted_at` и идемпотентен: повтор вернёт `deleted: false` и не перепишет метку времени
- `symptoms_list` отдаёт отметки за диапазон дат, обе границы включительно, не шире 400 дней
- `symptoms_save` заменяет день целиком. Пустой набор удаляет строку. Даты не позже завтрашнего по UTC. Незнакомый код, повтор или нарушенное взаимоисключение отбиваются до записи: сработавшая проверка базы записала бы симптомы в логи Postgres

Разрешённые адреса: `app.irenabio.com`, `*.vercel.app`, `*.netlify.app`, `*.github.io`, `localhost`, `web.telegram.org`, `t.me`.

Деплой функции только через CLI с флагом, сохраняющим выключенный `verify_jwt`. Деплой через MCP сбрасывает эту настройку и ломает вход.

**Переменные окружения:** `JWT_SECRET`, общий с `verify-access`, любое расхождение даёт вечный 401; `SUPABASE_URL`; `SUPABASE_SERVICE_ROLE_KEY`.

## 6. Вход

**Дверь 1, Телеграм.** `initData` уходит в `verify-access`, оттуда приходит токен и ложится в `localStorage` под общим для всех мини-апп Ирены ключом.

**Дверь 2, веб.** Токен и срок жизни приходят в хэше адреса, забираются при старте и сразу вычищаются из истории, чтобы не утекли вместе со ссылкой. Дверь активна только там, где нет `initData`.

Ответ 401 от `cycles-api` чистит токен и показывает экран с предложением подписки: на вебе он ведёт в приложение, в Телеграме в бота.

## 7. Открытые хвосты

- **ЗАКРЫТО 11.09.2026: разные диапазоны длины цикла.** Было: здесь фильтр 21-45, в автономной копии для ИИ-подружки (`../biohack/src/cycleContext.js`) 18-45, и на реальных данных подписчицы женщина видела 28 на экране и 24 у подружки. Закрыто парной правкой в обоих репозиториях: границы 15-45, окно 6 интервалов, `limit` 12, медиана вместо средней. Сверено программно на 3000 случайных наборов дат, расхождений 0. Копия осталась копией, контракт синхронизации в шапке `cycleCalc.js`. Осталось долгом: автоматическая сверка на дрейф, по образцу `tools/check-functions-drift.sh` в biohack. Пока копии держатся только на дисциплине.
- **Календарь и История расходятся в дне овуляции для закрытых циклов.** История считает фазы по фактической длине цикла, календарь по средней. Расхождение осознанное: сдвиг фертильного окна это продуктовое решение, а не хотфикс.
- **Главный экран при задержке пишет "Следующая ~" с датой в прошлом.** Прогноз это старт плюс средняя длина, без оглядки на сегодня.
- **Сетевой слой без страховки.** В `auth.js` один запрос без таймаута, повтора и запасного маршрута. Потерянный ответ даёт ложное "Не удалось удалить", хотя на сервере всё прошло. Данные не портятся: повтор идемпотентен.
- **Версию в @BotFather приходится поднимать руками** после каждого деплоя.

## 8. Правила работы в репозитории

- **Никаких длинных тире.** Нигде: код, комментарии, тексты интерфейса, надписи на картинке, сообщения коммитов. Только короткий дефис.
- **Соседние папки только для чтения.** Всё, что лежит рядом с `cycle`, это работающие мини-аппы с живыми подписчицами. Читать можно, писать, коммитить и запускать нельзя. Подробности в `CLAUDE.md`.
- **Маленькие шаги.** Сначала чтение и план в чате, правка только после явного "ок".
- **Никакого прямого обращения к базе из фронта.** Только через `cycles-api`. Публичный ключ Supabase в коде фронта не используется.
- **`_references/` не коммитится.**
- **Поднимать `?v=`** у изменённого файла в `index.html`, иначе правка не доедет до женщин.
- **Коды самочувствия в двух местах:** `SYMPTOM_OPTIONS` и `DISCHARGE_OPTIONS` в `app.js`, списки в `cycles-api`. Правка одного без другого это баг.

## 9. Подводные камни

- Пришёл 401 на свежий токен: сверить `JWT_SECRET` у `cycles-api` и `verify-access`.
- Блокируются запросы с нового адреса: дополнить список разрешённых в `edge-functions/cycles-api/index.ts` и передеплоить функцию.
- Случайно включился `verify_jwt`: вход умирает целиком.
- Идентификатор Telegram привязан к аккаунту, а не к устройству: один аккаунт на телефоне и на десктопе это один и тот же пользователь.
- `height: 100svh` на контейнере означает, что прокручивается внутренняя область, а не страница. Навигация всегда на экране.
- Локальный прогон только через `localhost`, не `127.0.0.1`. Список разрешённых адресов в `cycles-api` пускает `localhost`, а `127.0.0.1` получает 403: календарь пустой, ошибки на экране нет.
